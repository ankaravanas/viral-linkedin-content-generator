[build]
builder = "nixpacks"

[deploy]
startCommand = "python server.py"

[env]
APIFY_TOKEN = { $ref = "APIFY_TOKEN" }

# MCP servers run as workers, not web services
[experimental]
disableStaticSites = true
